---
namespace: trippsc2
name: nginx
version: 1.0.7
readme: README.md
authors:
  - Jim Tarpley
description: This collection contains roles for configuring NGINX.

license_file: 'LICENSE'

tags: []

dependencies:
  ansible.posix: '>=1.0.0'
  community.general: '>=1.0.0'
  trippsc2.general: '>=2.2.0'

repository: https://github.com/trippsc2/ansible-collection-nginx.git

build_ignore:
  - .gitignore
